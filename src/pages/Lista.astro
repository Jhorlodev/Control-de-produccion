---
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabase.jsx';

// Fetch de datos desde Supabase
const { data, error } = await supabase
  .from('control')
  .select('*');

if (error) {
  console.error('Error fetching data:', error);
}

// Calcular totales si hay datos
let totalRequerimiento = 0;
let totalHecho = 0;
let totalRestan = 0;

if (data) {
  totalRequerimiento = data.reduce((sum, item) => sum + (item.requerimiento || 0), 0);
  totalHecho = data.reduce((sum, item) => sum + (item.hecho || 0), 0);
  totalRestan = totalRequerimiento - totalHecho;
}
---

<Layout title="listado">
  <div class="containerp">
    <div class="container">
      <h1>Lista de Datos</h1>

<table>
  <thead>
    <tr>
      <th>Descripción</th>
      <th>Qty Total</th>
      <th>Qty Parcial</th>
      <th>Restan</th>
      <th>Fecha</th>
    </tr>
  </thead>
  <tbody>
    {data?.map((item) => (
      <tr>
        <td>{item.producto}</td>
        <td>{item.requerimiento}</td>
        <td>{item.hecho}</td>
        <td>{item.requerimiento - item.hecho}</td> {/* Calcula lo que falta */}
        <td>{item.fecha}</td>
      </tr>
    ))}
    {/* Fila de totales */}
  </tbody>
</table>  
</div>
  </div>
</Layout>

<style>
  :root {
    --dark-bg: #0f0f1a;
    --container-bg: #1a1a2e;
    --accent-purple: #7b1fa2;
    --light-purple: #bb86fc;
    --neon-blue: #4fc3f7;
  }

  .containerp {
    background-color: var(--dark-bg);
    min-height: 100vh;
    padding: 2rem;
    color: #e0e0e0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    margin-top: -150px;
    box-sizing: border-box;
  }

  .container {
    width: 90%;
    max-width: 800px;
    min-width: 300px;
    padding: 2rem;
    background-color: var(--container-bg);
    border-radius: 15px;
    box-shadow: 0 0 20px rgba(123, 31, 162, 0.3);
    border: 1px solid #33334d;
    overflow-x: auto;

  }

  h1 {
    color: var(--accent-purple);
    text-align: center;
    margin-bottom: 2rem;
    font-size: 2.5rem;
    text-shadow: 0 0 10px rgba(123, 31, 162, 0.5);
  }

  table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
    color: #e0e0e0;
  }

  th, td {
    border: 1px solid #44445a;
    padding: 12px;
    text-align: center;
  }

  th {
    background-color: var(--accent-purple);
    color: white;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  td {
    background-color: #2d2d44;
  }

  tr:nth-child(even) td {
    background-color: #33334d;
  }

  @media (max-width: 768px) {
    .container {
      padding: 1.5rem;
      width: 95%;
    }
    
    th, td {
      padding: 8px;
    }
    
    h1 {
      font-size: 2rem;
    }
  }

  @media (max-width: 480px) {
    .containerp {
      padding: 1rem;
    }
    
    th, td {
      padding: 6px;
      font-size: 0.9rem;
    }
    
    h1 {
      font-size: 1.5rem;
    }
  }
</style>